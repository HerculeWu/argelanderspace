// @argelanderspace/infra — side-effect adapters (mupdf, pandoc, HTTP sources,
// MinerU client, cheerio fetcher).
// Placeholder for M0 workspace topology; real adapters land in milestone M2.
export const PKG = "@argelanderspace/infra" as const;
