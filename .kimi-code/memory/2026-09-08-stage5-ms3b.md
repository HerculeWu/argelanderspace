# Stage 5 MS3b 旧管线代码全删 + 依赖清理 — 落地记录（2026-09-08）

MS3b 完成（四门全绿，未 commit——按 Q13 流程待对抗审查）。前置：
`2026-09-08-stage5-ms3a.md`（接线图与 MS3b 待删清单）。

## 已删（git status D 为准）

- **旧 LaTeX 管线**：`packages/core/src/pipelines/latex/`（walk/aastex/references/pipeline/
  ports/assets/fs-util 全目录）；`packages/infra/src/latex/{pandoc,pipeline,assets}.ts`
  （**保留 `arxiv-source.ts`** = 新管线 acquisition 层及其 `extractArxivSource`）；
  `packages/infra/src/pdf/`（mupdf raster）。
- **documents 死层**：`annotate.ts`、`citations.ts`、`crossrefs.ts`、`geom.ts`、`document.ts`
  （documentToJson/compact/buildIndex/stats 序列化机）。保留：ir.ts（buildDocIr, MS4 桥）、
  references.ts（parseOne，新管线用）、render.ts、tokens.ts、traverse.ts、pyregex.ts。
- **旧测试**：`core/tests/{golden-latex,latex,aastex}.test.ts`、`tests/helpers/latex-ports.ts`、
  `infra/tests/{pandoc,pandoc-version,latex-zip,mupdf-raster}.test.ts`；旧 golden
  `tests/golden/arxiv-*.json` + `manifest.json`；孤儿 fixtures `arxivpdf-1610.08981.pdf`、
  `infra/tests/fixtures/refs.bib`。
- **mupdf 依赖**：`packages/{infra,app}/package.json` 的 `"mupdf": "1.28.0"` 移除；
  `tsup.config.ts` 的 `external: ["mupdf"]` 移除；pnpm-lock 重生成。
- **contracts**：`api.ts` 的 `PaperResponseSchema`/`PaperResponse`（raw 路由别名）+ 路由文档行。

## 保留至 MS4（迁移后删除）

- `contracts/src/document.ts`（DocumentSchema）与 `core/src/documents/ir.ts`（buildDocIr）
  + 其依赖（tokens.ts 的 stripTokens、contracts 的 CITE_TOKEN_RE/XREF_TOKEN_RE）。
- 两个回退点：server `/api/paper/:id/ir`（无 version 字段 → buildDocIr 投影）、CLI
  `loadDocIr`（同逻辑）。**桥接 fixture**：`packages/core/tests/fixtures/document-arxiv-*.json`
  （两颗旧 Document golden 原位保留，ir.test.ts/render.test.ts/contracts golden.test.ts
  专用；JSON 无注释位，MS4 桥身份注明在各测试文件头注里——勿在 fixture 里找）。
- `documents/references.ts`（parseOne/matchKeys）、`render.ts`、`traverse.ts`、`pyregex.ts`、
  `--figure-dpi` 旗标（兼容忽略）。
- infra `latex/arxiv-source.ts`（含 `extractArxivSource` 的 tar 安全：穿越/符号链接拒绝——
  补了 `arxiv-source.test.ts` 的安全用例，evil/symlink fixtures 保留）。

## tsup banner 结论（重要，反直觉）

`external: ["mupdf"]` 已删，但 **createRequire banner 必须保留**：实测移除后 bundle 启动即炸
（`Dynamic require of "events" is not supported`——**`ws`（WebSocket）是 CJS**，运行期
`require("events")`，esbuild 的 `__require` stub 无 banner 时抛错）。原 banner 注释怪罪
safer-buffer/iconv-lite（cheerio 链，Stage 3.1 已死）——真活消费者是 `ws`，注释已更正。
另注意：app 包名是 `argelanderspace`（非 @argelanderspace/app），`pnpm --filter` 要按此名。

## 清理验证

- **pandoc shim 已退役**：全套测试在**无** `/tmp/ms1-bin` 的 PATH 下全绿。
- grep 清扫（`packages/*/src` + `tests`）：无 pandoc/mupdf 代码引用；残留仅历史注释
  （cite-format/text/walk 的"pandoc walker's semantics 移植"、lib/proc 的"used by pandoc
  adapter"、mdWithMath 的 texmath 边界规则出处——判定无害保留）。upload/refdetail/smoke
  测试里的 stub 字符串（"pandoc walk"/"pandoc exploded"/"pandoc could not parse"）已现代化。
- stale dist 坑：tsc 不清 dist 旧产物，删除源文件后**必须 rm -rf dist 重建**（app bundle
  一度内联了已删的 raster.js）。
- 回退实测：旧形 doc → CLI read ✓（buildDocIr 投影；`source: ?` 属过渡显示）+ server /ir ✓。
- E2E：battery ingest（真编译）→ read ✓。

## 测试计数（622 → 511）

contracts 39→**35**（golden.test.ts 重写：旧 golden+manifest 验收 → bridge fixture + 负例）、
core 229→**189**（删 golden-latex 2 / latex 14 / aastex 24 / documents 死套件 ~9，重写
documents.test→traverse+parseOne 2 例）、infra 84→**57**（删 pandoc/pandoc-version/latex-zip/
mupdf-raster 27 例）、web 105 / server 92 / cli 33 持平。

## 四门

`corepack pnpm -r build` ✓ / `-r typecheck` ✓ / `-r test`（**无 pandoc shim**）✓ / `lint` ✓。
