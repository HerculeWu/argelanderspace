%% Deliberately broken instrumentation package: forces the wrapper compile
%% to fail so the compile port must fall back to a clean compile.
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{argelander}[2026/09/04 deliberately broken test double]
\thismacroisnotdefined{with an argument
