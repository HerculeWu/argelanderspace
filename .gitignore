# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Node / frontend build
node_modules/
packages/*/dist/
*.tsbuildinfo
*.log

# M6 bundle package: copied into packages/app at build time (sources of
# truth live at the repo root — see packages/app/scripts/copy-assets.mjs)
packages/app/README.md
packages/app/LICENSE

# Tooling / indexes
.codegraph/
.serena/

# Generated ingestion outputs (regenerable; large binaries / fetch cache).
# The PDF sample under data/output/2603.03522 was committed earlier and stays
# tracked; this only ignores newly-generated artifacts (HTML docs, .htmlcache).
data/output/

# Library: local literature store + API enrichment cache (user data / regenerable)
data/library/

# Job runner runtime state (spooled uploads, job history)
data/jobs/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
