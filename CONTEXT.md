# ArgelanderSpace

单用户科研工作台，也是用户与 AI agent 协作的 interface。文献库、阅读、计划与写作是工作台的不同能力；webui 是可独立操作的应用，不只是 agent 看板。

## Language

### 文献与正文

**Work（文献条目）**：库中一项文献的书目身份，可以没有正文，也可以关联多个 Doc。
_Avoid_: 用 Doc 指代书目条目；用正文文件等同于一项文献。

**Doc（正文文档）**：一份已经摄入、可供阅读和寻址的具体正文；同一 Doc 可以被多个 Work 关联。
_Avoid_: Work；把每个 Doc 自动解释为发表版本。

**主 Doc**：一个 Work 默认打开的正文文档。主位是该 Work 对关联正文的选择，不代表系统判定的最新版本或正式发表稿。
_Avoid_: 自动最新稿、版本管理。

**Library（文献库）**：用户持久保存的 Work 集合及其关联信息，不包含尚未入库的探索候选。
_Avoid_: 将所有探索结果称作已收藏文献。

**Cite key（引用键）**：引用某个 Work 时使用的书目键，与 Work 的身份和 Doc 的正文寻址标识分别承担不同职责。
_Avoid_: Doc id。

**摄入（Ingest）**：把论文源材料处理为可读 Doc 的过程，不等同于只创建 Work 的书目条目。
_Avoid_: 将创建条目、下载源文件、摄入、挂载混称为同一操作。

**挂载（Attach）**：将具体 Doc 关联到已有 Work 的操作，不依靠标题相似来猜测用户选择的目标条目。

### 阅读与标注

**文档标注（Annotation）**：用户针对一个 Doc 中的目标位置留下的内容，归属于 Doc 而非 Work；高亮只是其显示方式。
_Avoid_: Writer comment、纯高亮。

**标注目标（Target）**：标注所指的整篇文档、结构块或一段文本位置，与标注正文分开。

**标注归档（Annotation archive）**：正文内容已确定更换后，保存旧一批标注的历史记录；不是正常 reader 或 agent 可继续读取的当前标注。
_Avoid_: 自动迁移后的标注、可自动重新定位的标注。

### 图谱与探索

**Library Graph（库图）**：已存 Work 之间的引用图，不包含推荐候选。
_Avoid_: 全局推荐图。

**Discovery（文献探索）**：从用户选择的一篇文献出发，查询相关文献及常用参考文献的临时探索过程；保存候选是单独的显式动作。
_Avoid_: 自动收藏、全库推荐。

**Discovery Graph（探索图）**：当前探索中的 seed、related、useful 文献及可见文献间真实引用关系构成的临时图。

**Related**：ADS 为当前 seed 返回的主题相关文献角色，不是一种图边。

**Useful**：ADS 基于本次实际 Related 集合返回的常用参考文献角色，不必然是奠基文献，也不是一种图边。
_Avoid_: 将 Useful 一律称作 Foundation。

**引用边（Citation edge）**：从引用者指向被引用文献的有向关系；similar 和 useful 都不产生另一种边义。

### 计划与写作

**Plan（科研计划）**：用户在工作台中管理的一组科研任务及其计划期限。
_Avoid_: 仓库实施规格、开发票据集合。

**Task（科研任务）**：属于某个 Plan、具有自身期限和状态的用户任务，不等同于本仓库的开发票据。

**Manuscript（稿件）**：用户在 Writer 中创作的独立稿件，不属于 Library 中的 Work 或已摄入的 Doc。
_Avoid_: 摄入论文、文献条目。

**Cell（稿件单元）**：构成 Manuscript 的可编辑内容单元，其类型表达该单元的写作用途。

**Template（稿件模板）**：描述稿件排版输入和可用单元类型的声明式模板，不是独立执行逻辑或自定义渲染器。

**Cell comment（单元便签）**：附在稿件单元上的纯文本便签，与 Doc 的 Annotation 分开。
_Avoid_: 文档标注、协作讨论线程。
